{
  "name": "qs4me-content",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build-index": "node scripts/build-index.mjs",
    "build-bundle": "node scripts/build-bundle.mjs",
    "build": "pnpm validate && pnpm build-index && pnpm build-bundle",
    "collect": "node scripts/collect.mjs",
    "catalog": "node scripts/build-catalog.mjs",
    "catalog:serve": "node scripts/build-catalog.mjs && node scripts/catalog-server.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "sharp": "^0.33.5"
  }
}
